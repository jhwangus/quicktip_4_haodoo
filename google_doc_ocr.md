# 📚 使用 Google Docs 進行批次 OCR 指南

Google Docs 擁有一項強大的隱藏功能：當初次開啟 PDF 檔案時，系統會自動執行 **OCR（光學字元辨識）**。本流程利用此特性，配合 Python 自動化腳本，能大幅提升電子書製作效率。

### 📌 核心限制

* **檔案大小**：必須小於 **40MB**。
* **頁數限制**：建議少於 **80 頁**（超過則僅插入圖像而不執行 OCR）。
* **適用性**：確認支援直排與橫排書籍。

### 本流程利用此隱藏功能，具有優點如下

* **極速辨識**：300 頁書籍的處理時間從 4 小時縮短至 **4～5 分鐘**。
* **避免遺漏**：批次處理 PDF 比單張處理圖像更穩定。
* **排版友善**：自動將英文標點符號轉為中文標點符號，方便 mPDB 做直排處理。

---

## 一、 準備工作：分割 PDF 或組合圖像

為了符合 Google Docs 的限制，需先將書籍內容切分為適當大小。

### 方法 A：使用 PDFsam Basic 分割現有 PDF

1. **安裝**：下載並安裝 [PDFsam Basic](https://pdfsam.org/download-pdfsam-basic/)。
2. **執行**：選擇 **Split** 功能。
3. **設定**：選擇 `Split by every "n" pages`，並輸入 **50**。
4. **輸出**：確認分割後的檔案是否小於 40MB，若太大則調低頁數（如 40）。

### 方法 B：將多個掃描檔轉為 PDF (ImageMagick)

1. **安裝**：下載 [ImageMagick](https://imagemagick.org/script/download.php) 並安裝。
2. **批次處理**：將附錄中的 `img2pdf.bat` 存至圖像目錄。
3. **執行**：於命令列執行 `img2pdf.bat`，程式會自動將圖像每 50 張打包成一個 PDF。
4. **手動指令**：
```bash
convert 0000* 0001* 0002* 0003* 0004* book1.pdf

```

---

## 二、 執行 Google Docs OCR

1. **上傳**：將 PDF 檔上傳至 Google Drive。
2. **轉換**：在檔案上按右鍵，選擇 **「選擇開啟工具」 -> 「Google 文件」**。
3. **清理格式**：
   * 轉換後的文字通常字型極大，請按 `Ctrl + A` 全選。
   * 然後按 `Ctrl + \\` （或點選選單：**「格式」 -> 「清除格式」**）即可恢復正常版型。

---

## 三、 快速轉換標點與英數

> [!CAUTION]
> 本章節之轉換工具與邏輯旨在將半形標點與英數強制轉為全形，以利直式排版。

透過 Python 腳本 `ocr_fix.py`，可以一鍵處理剪貼簿中的文字，避免 mPDB 誤刪空白。

### 1. 安裝環境

* 安裝 [Python 3](https://www.python.org/downloads/)。
* 安裝必要套件：`pip install pyperclip`。

### 2. 建立 ocr_fix.py

將以下邏輯儲存為 `D:\py\ocr_fix.py`（路徑可自訂）：

```python
import pyperclip

# 定義搜尋與取代對應表
pairs = [
    (" ", "　"), (",", "，"), (";", "；"), (":", "："), ("?", "？"), ("!", "！"),
    ("(", "（"), (")", "）"), ("...", "…"), ("·", "・"),
    ("A", "Ａ"), ("B", "Ｂ"), ("C", "Ｃ") # ... 以此類推
]

text = pyperclip.paste()
for old, new in pairs:
    text = text.replace(old, new)
pyperclip.copy(text)

```

### 3. 操作流程

1. **複製**：在 Google Doc 中 `Ctrl + A` 全選，`Ctrl + C` 複製。
2. **執行**：雙擊桌面上的 **OCR_FIX** 捷徑。
3. **貼回**：回到編輯器按 `Ctrl + V`，文字即完成全形轉換。

---

## 📖 附錄：自動化腳本工具

### img2pdf.bat 原始碼

此批次檔能自動將 `000xxx.jpg/png` 格式的圖像每 50 頁打包，最高支援至 999 頁。

```batch
@echo off
setlocal ENABLEDELAYEDEXPANSION

rem (C)2024, Jack Hwang, All Rights Reserved
set num1=0,1,2,3,4
set num2=5,6,7,8,9

for /L %%i in (0,1,9) do (
   set var=
   for %%j in (%num1%) do set var=!var! 000%%i%%j*
   echo convert !var! book%%i_1.pdf
   convert -quiet !var! book%%i_1.pdf 2> nul
   
   set var=
   for %%j in (%num2%) do set var=!var! 000%%i%%j*
   echo convert !var! book%%i_2.pdf
   convert -quiet !var! book%%i_2.pdf 2> nul
)

```

---

